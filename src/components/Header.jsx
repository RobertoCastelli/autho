import React from "react";

const Header = () => {
  return (
    <header>
      <h1>AUTH</h1>
      <h3>React + Firebase authentication test</h3>
      <small>back to the guide</small>
    </header>
  );
};

export default Header;
